## Production: use same-origin for API calls
## Leave VITE_API_BASE_URL undefined so frontend calls window.location.origin
## The IIS web.config will reverse-proxy /api to the backend.
# Intentionally empty
